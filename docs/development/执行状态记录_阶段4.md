# 项目执行状态记录

记录时间：2026-09-13（Asia/Shanghai）

## 当前结论

GitHub `main` 当前最新提交为 `d17def7489306308ac5378f575624fe6cea11d99`，提交说明为
`Complete first-stage attendance and personnel modules`。该远端快照已经包含阶段 0 至阶段 14 的代码形态；本地同步后已完成 140 个远端文件的 SHA-256 校验。

项目当前应先完成本机阶段 15 的 APK 构建验收，再决定是否进入新的开发任务。执行说明书中的阶段顺序与 GitHub 当前代码进度不完全一致：远端已经包含阶段 5 及以后模块，但本机尚未独立复核 APK 构建与安装。

## 阶段进度

- 阶段 0：项目初始化，已完成。
- 阶段 1：数据库底座，已完成；当前 `schemaVersion` 为 8，已包含人员附件表。
- 阶段 2：人员档案，已有页面、Repository 和测试。
- 阶段 3：考勤组，已有页面、Repository 和测试。
- 阶段 4：月度考勤名单，已有月份/考勤组选择、添加、移除、重新添加、上月复制和测试。
- 阶段 5：每日考勤，远端已有页面、Provider、Repository 和测试。
- 阶段 6：月考勤表，远端已有页面、Repository、半天统计和测试。
- 阶段 7：请假，远端已有列表、表单、联动考勤和测试。
- 阶段 8：加班，远端已有分钟数保存、展示、Repository 和测试。
- 阶段 9：离职，远端已有登记、撤销、历史保留、停保提醒和测试。
- 阶段 10：月度汇总，远端已有统计、异常检查、确认/锁定和测试。
- 阶段 11：社保保险，远端已有参保档案、变更、基数历史页面和 Repository。
- 阶段 12：Excel，远端已有人员导入及多类报表导出服务和测试。
- 阶段 13：提醒，远端已有本地提醒设置、列表和测试。
- 阶段 14：备份与恢复，远端已有 `.qsbak` 校验、备份服务和测试。
- 阶段 15：代码检查已通过；本机 APK 构建未完成，真机/模拟器安装未复核。

## 本次 UI 与附件任务

- 首页看板和六个快捷入口已补齐，人员、考勤、设置页面过期阶段文案已清理。
- 考勤页已增加月度汇总入口，月度汇总页已增加当前月份 Excel 导出按钮。
- 已新增统一人员附件中心，接入人员详情、离职材料和保险材料，并升级备份包格式为 2。
- 当前开发分支为 `codex/ui-entry-attachments`，基线提交为 `1eeb400`；原有 `stash@{0}` 保持不变。

## 环境状态

- Flutter stable 3.47.4，Dart 3.13.3。
- JDK 17：`D:\houduanruanjian\jdk17`。
- Android SDK：`D:\Android\sdk`，Platform 35 和 Build Tools 35.0.0 已存在。
- `flutter doctor -v`：通过，显示 `No issues found!`。
- `flutter pub get`：通过。
- 当前无 Android 真机或模拟器连接。
- NDK `28.2.13676358` 尚未完整安装；项目使用 `flutter.ndkVersion`，这是 APK 构建的当前阻塞点。

## 当前暂停任务

- 任务：Android NDK r28c 下载。
- JobId：`9cf45197-9e73-4738-a21a-1a79b598937b`。
- 状态：`Suspended`，未取消。
- 进度：81,753,536 / 748,118,221 字节，约 10.9%。
- 暂停后未恢复下载，也未执行 APK 构建。

## 本机验证结果

- `flutter analyze`：通过，无问题。
- `flutter test`：通过，73 项全部通过。
- `flutter build apk --debug`：本机尚未通过；当前没有 APK 输出文件，不能将远端验收文档中的“构建/安装通过”视为本机复核结果。
- 仓库内的 `第一阶段验收结果.md` 记录了远端历史验收结果，但其中环境版本为 Flutter 3.47.2、Dart 3.13.2，且使用过 `emulator-5554`，与当前本机状态不同。

## 同步与本地改动保护

- GitHub 快照已下载并同步到 `D:\QingSongBan`。
- 本地 Git CLI 因缺少 HTTPS 凭据，未能建立 `origin/main` 远端跟踪引用；工作区文件内容已与 GitHub 快照校验一致。
- 同步前的本地阶段 4 未提交改动已保存到 `stash@{0}`，未丢失。
- 当前同步内容尚未提交到本地 Git；工作区存在远端快照对应的未提交变更。

## 恢复后的执行顺序

1. 经确认后恢复 JobId `9cf45197-9e73-4738-a21a-1a79b598937b` 的下载。
2. 完成并安装 NDK `28.2.13676358`。
3. 执行 `flutter build apk --debug`。
4. 无 Android 设备时完成 APK 构建验证；有设备时再执行安装与冷启动验证。
5. 根据阶段 15 验收结果决定后续维护或新开发任务，不自动扩大范围。
