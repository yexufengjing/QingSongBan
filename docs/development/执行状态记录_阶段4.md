# 项目执行状态记录

记录时间：2026-09-13（Asia/Shanghai）

## 当前结论

GitHub `main` 当前最新提交为 `74d99bb2dfcc0bb865f23abc027189a323d0286f`，提交说明为
`Implement temporary worker payroll`。该远端快照已经包含阶段 0 至阶段 14 及临时工薪资模块；本地已通过 Git 快进同步。

项目已完成第一阶段和临时工薪资模块的本机代码验收；Android APK 构建通过，并已在 Android 15 API 35 模拟器完成安装和人工冷启动复核。

## 阶段进度

- 阶段 0：项目初始化，已完成。
- 阶段 1：数据库底座，已完成；当前 `schemaVersion` 为 9，已包含人员附件和临时工工资表。
- 阶段 2：人员档案，已有页面、Repository 和测试。
- 阶段 3：考勤组，已有页面、Repository 和测试。
- 阶段 4：月度考勤名单，已有月份/考勤组选择、添加、移除、重新添加、上月复制和测试。
- 阶段 5：每日考勤，远端已有页面、Provider、Repository 和测试。
- 阶段 6：月考勤表，远端已有页面、Repository、半天统计和测试。
- 阶段 7：请假，远端已有列表、表单、联动考勤和测试。
- 阶段 8：加班，远端已有分钟数保存、展示、Repository 和测试。
- 阶段 9：离职，远端已有登记、撤销、历史保留、停保提醒和测试。
- 阶段 10：月度汇总，远端已有统计、异常检查、确认/锁定和测试。
- 阶段 11：社保保险，远端已有参保档案、变更、基数历史页面和 Repository。
- 阶段 12：Excel，远端已有人员导入及多类报表导出服务和测试。
- 阶段 13：提醒，远端已有本地提醒设置、列表和测试。
- 阶段 14：备份与恢复，远端已有 `.qsbak` 校验、备份服务和测试。
- 临时工薪资：已完成工种与日薪历史、人员工资资料、名单生成/排序、工资核算、考勤同步、异常检查、确认/锁定、历史和 Excel 导出。
- Android Excel 导出已改为系统 `Download`（下载）目录，避免文件只写入应用私有目录而无法查找。
- 工种设置已补充停用后的“启用工种”入口，并统一优化修改工种、添加生效日薪弹窗；生效月份改为年份下拉与月份快捷选择。
- 工资确认已修复：考勤快照不再将汇总状态、记录 ID 和更新时间误判为考勤变更；确认/解锁输入弹窗不会因键盘收起触发控制器生命周期异常。
- 阶段 15：`flutter analyze`、`flutter test` 和本机 APK 构建均已通过；已在 Android 15 API 35 模拟器完成安装与人工 UI 复核。

## 本次 UI 与附件任务

- 首页看板和工资快捷入口已补齐，人员、考勤、设置页面过期阶段文案已清理。
- 考勤页已增加月度汇总入口，月度汇总页已增加当前月份 Excel 导出按钮。
- 已新增统一人员附件中心，接入人员详情、离职材料和保险材料，并升级备份包格式为 2。
- 临时工工资已接入汇总页、首页快捷操作、人员工资记录和本月考勤跳转；工资首页与造资页支持考勤组筛选，工资明细显示参保状态。

## 环境状态

- Flutter stable 3.47.2，Dart 3.13.2。
- JDK 17：`D:\houduanruanjian\jdk17`。
- Android SDK：`D:\Android\sdk`，Platform 35 和 Build Tools 35.0.0 已存在。
- `flutter doctor -v`：通过，显示 `No issues found!`。
- `flutter pub get`：通过。
- 中文演示数据：使用 `flutter run -d emulator-5554 --dart-define=QSB_SEED_DATA=true` 首次启动加载，数据种子具备幂等标记。
- 当前已连接 Android 15 API 35 模拟器 `emulator-5554`。
- 模拟器系统首选语言已设置为中文（中国）。
- APK 构建已通过，NDK 不再构成当前构建阻塞点。

## 当前暂停任务

- 无。工资设置页面的模拟器人工 UI 复核已完成。

## 本机验证结果

- `flutter analyze`：通过，无问题。
- `flutter test`：通过，98 项全部通过。
- `flutter build apk --debug`：通过，产物为 `build/app/outputs/flutter-apk/app-debug.apk`。
- Android 设备 / 模拟器：已使用 `emulator-5554` 完成安装、冷启动和工资设置页面人工复核。
- Android 设备 / 模拟器：已完成“月度汇总确认 → 工资确认”人工流程，工资批次成功显示为“已确认”。
- Android 设备 / 模拟器：已完成工资 Excel 导出人工验证，文件出现在系统 `Download`（下载）目录。

## 同步与本地改动保护

- GitHub `origin/main` 已通过 Git CLI 快进同步到 `74d99bb`。
- 当前工作区保留本次验证与业务补强的未提交变更，未覆盖或删除用户原有文件。

## 后续可选验收

1. 执行工资入口、名单生成、编辑、确认/锁定、历史和导出的完整业务人工复核。
